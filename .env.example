# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers
# For local development, set the callback URL to: http://localhost:4321/api/auth/callback

# GitHub OAuth App Client ID
GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub OAuth App Client Secret
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# OAuth Callback URL (must match GitHub OAuth App settings)
GITHUB_CALLBACK_URL=http://localhost:4321/api/auth/callback

# Session Secret - REQUIRED for production
# Used to sign and verify session cookies
# SECURITY: Generate a strong random secret for production with:
#   openssl rand -base64 32
# WARNING: Never commit your real secret to version control!
SESSION_SECRET=your_random_secret_here_change_in_production

# Organization Mode (OPTIONAL)
# If set, restricts the dashboard to only show workflows from this GitHub organization
# Leave empty or remove to show workflows from all repositories the user has access to
# Example: GITHUB_ORG=my-company
GITHUB_ORG=

# Custom Logo (OPTIONAL)
# URLs to custom logo images to replace the default logo
# When set, displays your logo with "Actions Hub" branding
# Use IMAGE_URL_LIGHT for light mode and IMAGE_URL_DARK for dark mode
# If only one is set, it will be used for both modes
# Example: IMAGE_URL_LIGHT=https://example.com/logo-dark-text.png
# Example: IMAGE_URL_DARK=https://example.com/logo-light-text.png
IMAGE_URL_LIGHT=
IMAGE_URL_DARK=